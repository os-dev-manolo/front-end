export enum SearchParamsEnum {
    ORDER = "orderBy",
    WHERE = "where",
    SELECT = "select",
    LIMIT = "limit",
    OFFSET = "offset",
    SELECT_DISTINCT = "select_distinct",
}

export enum SearchParamsScreenEnum {
    FILTERS = "filters",
    REGISTER = "register",
    EDIT = "edit",
    CLONE = "clone",
    DETAILS = "details",
    FIELDS = "fields",
}
